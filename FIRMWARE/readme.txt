Upload the firmware with X loader 
https://github.com/xinabox/xLoader

This firmware is only for generate a ID code that you can see on tft display, then i give you a fw unlocked.

Then format the SD with SDfat formatter repository and save on the SD the database.txt file that contain all the most TTL algorithm
